# ============================================================
# Weatherise Backend — requirements.txt
# Python 3.12 | Windows + Linux compatible
# ============================================================

# --- Web Framework ---
fastapi==0.115.12
uvicorn[standard]==0.34.3
starlette==0.46.2
python-multipart==0.0.20

# --- Database ---
SQLAlchemy==2.0.41
asyncpg==0.30.0
greenlet==3.2.3

# --- Redis (sync client for non-async contexts) ---
redis>=5.0.0,<6.0.0

# --- Pydantic / Settings ---
pydantic==2.11.5
pydantic-core==2.33.2
pydantic-settings==2.9.1
pydantic-extra-types==2.10.3
annotated-types==0.7.0

# --- HTTP / Networking ---
httpx==0.28.1
httpcore==1.0.9
anyio==4.9.0
h11==0.16.0
certifi==2025.4.26
idna==3.10
urllib3==2.4.0
dnspython==2.7.0

# --- Auth ---
pyjwt==2.10.1

# --- Data / Parsing ---
PyYAML==6.0.2
python-dotenv==1.1.0
email-validator==2.2.0
async-timeout==5.0.1

# --- CLI / Dev Tools ---
click==8.2.1
typer>=0.16.0
shellingham==1.5.4
rich==14.0.0
rich-toolkit>=0.19.7
Pygments==2.19.1
markdown-it-py==3.0.0
mdurl==0.1.2
MarkupSafe==3.0.2
Jinja2==3.1.6

# --- Misc ---
exceptiongroup==1.3.0
tomli==2.2.1
colorama==0.4.6
watchfiles==1.0.5
websockets==15.0.1

# --- Agent Pipeline (Phase 4) ---
# NOTE: Pin langgraph + langchain to compatible version sets
langchain-core>=0.3.0,<0.4.0
langchain-openai>=0.2.0,<0.3.0
langgraph>=0.2.0,<0.3.0
langgraph-checkpoint-redis>=0.0.1
openai>=1.40.0,<2.0.0

# --- Vector DB ---
pymilvus>=2.4.0,<3.0.0

# --- EXCLUDED: nemoguardrails (requires annoy → C++ build tools on Windows) ---
# nemoguardrails>=0.10.0

# --- EXCLUDED: uvloop (Linux only, not available on Windows) ---
# uvloop==0.22.1
